# response_agent.py: suggests safe routes / logs events (placeholder)
def suggest_safe_place(lat, lon):
    # In v0, return a static suggestion; extend with OSM queries later.
    return {"name":"Nearest Police Help (example)", "lat":lat, "lon":lon}
